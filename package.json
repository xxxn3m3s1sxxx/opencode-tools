{
  "name": "opencode-tools",
  "version": "0.2.0",
  "description": "Reliability toolbox for AI-assisted coding — hashline + impact + verify + trace",
  "keywords": ["opencode", "plugin", "hashline", "impact", "verify", "trace", "edit", "analysis"],
  "license": "MIT",
  "type": "module",
  "files": [
    "hashline.ts",
    "hashline.py",
    "impact.ts",
    "impact.py",
    "verify.ts",
    "verify.py",
    "trace.ts",
    "trace.py",
    "README.md"
  ],
  "scripts": {
    "test": "python hashline.py --version && python impact.py --version && python verify.py --version && python trace.py --version"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^0.1.0",
    "@opencode-ai/sdk": "^0.1.0"
  }
}
